/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i0.wp.com", "jasonsmovieblog.com", "shared.akamai.steamstatic.com"],
    },
};

export default nextConfig;
